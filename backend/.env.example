#DEVMODE=enabled
APPSECRET=KoudPils
#DEFAULT_USER=
#DEFAULT_PASS=
DB_HOST=
DB_USERNAME=
DB_PASSWORD=
DB_NAME=
PORT=3000
# Passkeys are bound to these. RP_ID is the bare domain, RP_ORIGIN the exact
# site the browser must be on. Left unset they are taken from the request,
# which is fine on localhost but should be pinned anywhere else.
#RP_ID=pils.oscarr.nl
#RP_ORIGIN=https://pils.oscarr.nl
